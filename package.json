{
  "name": "gh",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --compile --define process.env.APP_VERSION=\"'${APP_VERSION:-dev}'\" --outfile dist/gh",
    "install:ubuntu": "bun run build && sudo install -m 755 dist/gh /usr/local/bin/gh"
  },
  "devDependencies": {
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/bun": "latest",
    "@types/debug": "^4.1.13",
    "@types/rc": "^1.2.4",
    "semantic-release": "^23.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.2.0",
    "debug": "^4.4.3",
    "rc": "^1.2.8",
    "zod": "^4.3.6"
  },
  "version": "1.2.0"
}
