{
  "name": "gh",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile dist/gh",
    "install:ubuntu": "bun run build && sudo install -m 755 dist/gh /usr/local/bin/gh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/rc": "^1.2.4",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^23.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.2.0",
    "rc": "^1.2.8",
    "zod": "^4.3.6"
  },
  "version": "1.1.0"
}
